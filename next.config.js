/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: 'export', // Uncomment this line if deploying to GitHub Pages
    images: {
        unoptimized: true,
    },
}

module.exports = nextConfig
